# JobRunr demo with Action Executor jobs

### JobRunr dashboard:  
Enable it in application.properties by setting ``org.jobrunr.dashboard.enabled`` to ``true``

Dashboard address:
```http://localhost:1000```